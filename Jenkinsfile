pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code downloaded successfully'
            }
        }

        stage('Build') {
            steps {
                echo 'Build Successful'
            }
        }
    }
}
